import React from "react";
import { Box } from "@mui/material";
const Logo = () => {
    return (
        <Box
            sx={{
                height: "5vh",
                display: "flex",
                justifyContent: "center",
                alignItems: "center",
            }}
        >
            Logo
        </Box>
    );
};

export default Logo;
